{
  "name": "plza",
  "version": "0.1.0",
  "description": "Pokemon Legends: Z-A Interactive Map",
  "type": "module",
  "scripts": {
    "dev": "bun run server.ts",
    "build": "bun build src/main.ts --outdir public --minify --target browser --entry-naming [name].js",
    "preview": "cd public && python3 -m http.server 8000"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.8",
    "typescript": "^5.3.3"
  }
}
